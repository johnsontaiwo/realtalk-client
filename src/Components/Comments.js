import  React, { Component }  from 'react';
import CommentContainer from './CommentsContainer'
import Comment from './Comment'
import Article from './Article'

class Comments extends Component {


  render() {
    //debugger
//const filteredComments =  this.props.comments.comments.map(comment => <Comment key={comment.id} comment={ comment }  articleID={this.props.articleId} deleteComment={this.props.deleteComment} /> )
    //debugger
    return(
      <div>
         
    </div>
      )

  }
}

export default Comments